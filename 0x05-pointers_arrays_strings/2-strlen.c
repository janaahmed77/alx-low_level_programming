#include <stdio.h>

/**
 * _strlen - to swap value
 * @s: The integer to be printed
 * Return: Lenght
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
	length++;
	s++;
	}
	return (0);
}
