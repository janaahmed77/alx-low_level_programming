#include "main.h"
#include <stdio.h>
/**
 * *print_sign - print_sign
 * @n: The loweer to checked
 * main - check the code.
 * Return: Always 0.
 */
int print_sign(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-(n));
	}
}
