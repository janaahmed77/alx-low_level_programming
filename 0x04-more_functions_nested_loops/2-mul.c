#include "main.h"

/**
 * mul - multiply a and b
 *  * @a: mutliply
 *  * @b: mutliply
 * Return: 1 if c Upercase else 0
 */

int mul(int a, int b)
{
	int a;
	int b;
	mul = a * b;
	printf("%d", mul);
	return (mul);
}
