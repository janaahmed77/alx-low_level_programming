#include "main.h"
/**
 * print_numbers - check the code.
 * Return: Always 0.
 */

void print_most_numbers(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
	{
	if (c == 2 || c == 4)
	{
	continue;
	}
	_putchar (i + '0');

	}
	_putchar ('\n');
}
